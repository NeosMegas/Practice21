@{
    ViewData["Title"] = "Вход";
}
@model User
@* <form method="post">
    <div class="form-group">
        <label for="login">Имя пользователя</label>
        <input type="text" class="form-control" id="login" aria-describedby="loginHelp" placeholder="Имя пользователя">
        <small id="loginHelp" class="form-text text-muted">Ваше имя пользователя</small>
    </div>
    <div class="form-group">
        <label for="password">Пароль</label>
        <input type="password" class="form-control" id="password" placeholder="Пароль">
    </div>
    <input type="submit" value="Войти" asp-controller="Home" asp-action="Login"/>
    <button type="submit" class="btn btn-primary" asp-controller="Home" asp-action="Login">Войти</button>
</form> *@

@using(Html.BeginForm("Login", "Home", FormMethod.Post))
{
    <div class="form-group">
        <label for="Login">Логин</label>
        @Html.TextBoxFor(u => u.Login, new { @class = "form-control", @placeholder = "Логин", autofocus = "autofocus" })
    </div>
    <div class="form-group">
        <label for="Password">Пароль</label>
        @Html.PasswordFor(u => u.Password, new { @class = "form-control", @placeholder = "Пароль" })
    </div>
    <button type="submit" class="btn btn-primary">Войти</button>
}
